#pragma once
// Streaming-only categories for the settings panel: Stats, Apps, Launch.
// Built as MK_CUSTOM categories, content positioned in builder-local metres.
#include "menu_model.h"

void buildStreamCategories(MenuModel &m);
