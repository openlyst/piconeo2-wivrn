# CodeRabbit: only run on manual request.
# Trigger a review by commenting `@coderabbitai review` on a PR/MR.
# Auto-review on every push/open is disabled.
reviews:
  auto_review:
    enabled: false
    drafts: false
  request_changes_workflow: false
  high_level_summary: true
